# 模板配置文件 - 使用环境变量占位符
# 复制此文件并根据环境重命名为 .env.dev, .env.online 等

# ============================================
# Telegram Bot 配置
# ============================================
TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
TELEGRAM_CHANNEL_ID=${TELEGRAM_CHANNEL_ID}

# ============================================
# MySQL 数据库配置
# ============================================
# 格式: mysql://username:password@host:port/database
DATABASE_URL=mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@${MYSQL_HOST}:${MYSQL_PORT}/laohuangli

# 数据库连接池配置（可选，sqlx 默认值通常足够）
# DATABASE_POOL_MIN_CONNECTIONS=5
# DATABASE_POOL_MAX_CONNECTIONS=20
# DATABASE_POOL_ACQUIRE_TIMEOUT=60
# DATABASE_POOL_IDLE_TIMEOUT=300
# DATABASE_POOL_MAX_LIFETIME=900

# ============================================
# Redis 配置
# ============================================
# 格式: redis://[password@]host:port[/database]
REDIS_URL=redis://127.0.0.1:6379/0
# 如果 Redis 有密码，格式为: redis://:password@host:port/database
# REDIS_URL=redis://:password@127.0.0.1:6379/0

# Redis 连接超时（毫秒，可选）
# REDIS_TIMEOUT=1000

# ============================================
# 日志配置
# ============================================
# 日志级别: trace, debug, info, warn, error
RUST_LOG=info
# 可以设置特定模块的日志级别，例如:
# RUST_LOG=laohuangli_bot=debug,laohuangli_bot::database=debug

# ============================================
# 应用配置
# ============================================
# 应用名称
APP_NAME=laohuangli_bot


